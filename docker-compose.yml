services:
  web:
    build: .
    env_file:
      - .env
    restart: unless-stopped
  
  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel run
    volumes:
      - ./tunnel:/etc/cloudflared
    env_file:
      - .env
    restart: unless-stopped