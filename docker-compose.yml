services:
  web:
    build: .
    environment:
      - NODE_ENV=development
  
  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel run
    volumes:
      - ./tunnel:/etc/cloudflared
    env_file:
      - .env